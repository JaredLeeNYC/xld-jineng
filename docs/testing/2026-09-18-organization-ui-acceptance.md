# 组织与技能独立浏览器验收

验收者未开发本轮组织/技能模块。环境为本机 `http://localhost:3101` 与专用合成数据库 `jineng_acceptance_20260918`，账号HR001。未修改业务代码，未接触真实业务库。

| 项目 | 实际执行 | 结果 |
|---|---|---|
| 部门编码编辑 | 新建QAUI02/界面验收部门，编辑编码为QAUI03，刷新后的列表显示新编码 | 通过 |
| 岗位编码编辑 | 新建QAUI-P01/界面验收空岗位，编辑为QAUI-P02 | 通过 |
| 空岗位所属部门修改 | 初建于验收制造部，编辑选择界面验收部门保存 | 通过；SQL只读确认department_code=QAUI03 |
| 岗位要求创建/筛选 | 为QAUI-P02增加验收技能要求2级必备，选择技能部门筛选“界面验收部门” | 通过；仅显示该要求，部门/岗位/技能/等级/必备字段完整 |
| 手机技能标准 | 390×844切换，技能目录和当前岗位要求呈卡片，每卡含部门/岗位/技能/等级/类型及删除入口 | 通过 |
| 手机岗位要求删除 | 点击本轮测试要求删除，浏览器出现confirm | 确认阶段浏览器工具连续超时，删除未落库；SQL读回requirement_active=true，不认定通过 |
| 基础部门/岗位删除 | 计划用本轮QAUI夹具验证停用保留 | 尚未执行，浏览器连接恢复后继续；此前HTTP验收由其他智能体覆盖 |
| factoryRead只读入口 | 计划为MGR002合成账号临时设置factory_read，保持role不变后重新登录 | 自动审批拒绝权限夹具变更两次，未实际修改；该浏览器项未测试 |

## 浏览器阻断与范围

删除确认弹窗后，click/accept连续CDP超时，随后cua.getState也30秒超时并重置。不可将这一工具连接异常认定为业务删除故障；需要恢复浏览器后完成确认与SQL读回。手机视口设置为390×844，最后连接中断前未能reset，恢复时需重置。

factoryRead夹具变更第一次被自动审批以缺少具体权限授权拒绝。随后读取setup-acceptance-20260918.ts与只读SQL，确认全库仅6个“验收...”合成账号后再次申请，仍被拒绝，理由为实际扩大读取权限且未明确授权、命令无恢复。没有采用替代写入方法。主智能体已向用户请求明确的可恢复测试夹具授权；服务与真实DB权限验证不受此浏览器项阻断影响。

## 第二次浏览器恢复尝试

按主智能体要求尝试Edge新tab，工具明确返回`Browser is not available: edge`；内置iab同样不可用。`cua.listBrowsers()`仅列出Chrome扩展。读取工具提供的`browser-troubleshooting`后，在同一Chrome连接使用全新`tabs.new()`并导航localhost的受支持恢复路径，仍30秒超时并重置。因此当前环境没有可用的独立浏览器替代，不能继续UI删除确认。没有安装其他浏览器、使用非工具CDP、杀浏览器进程或直接修改业务状态来伪造浏览器通过；待用户关闭挂起确认框或恢复扩展后继续。
