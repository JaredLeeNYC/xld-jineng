# 独立验收第一轮（2026-09-08）

结论：**未通过，返修中**。在隔离数据库 `jineng_acceptance_20260908` 和本地真实 HTTP 服务、Chrome 浏览器执行。全部业务夹具为验收合成数据。

## 门禁与已执行证据

- 独立 `bun run check`：退出 0，84 文件无类型/lint错误，OpenAPI/部署契约通过，真实 PostgreSQL 合同通过，前端生产构建通过。
- 独立 `bun test`：退出 0，121 pass、0 fail、388 断言，29 文件。
- 完整字段员工 Excel 导入实际经过 dry-run→confirm→HTTP列表读回：主管角色、姓名、手机、性别、年龄、身份证号文本、入职日期、部门、岗位、司龄、学历均保存正确。
- 主管三种培训类型创建、发布成功。撤回→修改地点→再发布，原任务 ID 保留，9 个活动任务无重复。
- API详细合成夹具结果见 [API证据](2026-09-08-api-evidence.json)。UI与Office真实转换仍在验收，不提前计为通过。

## R1-001：HR手工创建主管实际创建为普通员工

- 严重度：高；Excel 第3行。
- 步骤：HR001 登录；调用 `POST /api/organization/employees`，填写合法部门/岗位，工号 `QA-MANUAL`、角色 `department_manager`；从数据库关联 `employees` 和 `user_accounts` 读取角色。
- 预期：成功创建部门主管账号，读回 `department_manager`。
- 实际：HTTP 200，但真实持久化角色为 `employee`。
- 证据：独立API测试输出 `FAIL HR手工新增主管角色真实落库 {"status":200,"actualRole":"employee"}`。
- 原因定位：员工创建 HTTP body schema 遗漏 `role`；同一 role schema 误加在登录接口中，service单元测试无法发现此边界。
- 返修要求：将角色契约补到正确创建接口，移除登录无关字段；补充真实HTTP请求落库测试；独立验收以新工号重新复测。

## 验收范围说明

## R1-002：主管手机任务卡片遗漏新增部门

- 严重度：中；Excel 第24行，兼顾第12/21行时间显示。
- 步骤：MGR001登录，浏览器视口390×844，进入培训管理，查看培训任务与待办卡片；按员工、状态筛选已确认记录。
- 预期：手机卡片展示部门及与桌面一致的培训类型、开始/截止业务时间。
- 实际：任务卡片只有计划、状态、员工姓名、截止日期；桌面表格有部门，手机没有。培训计划卡片部门正常。
- 证据：[手机任务卡片快照](2026-09-08-evidence/mgr-mobile-task-before.txt)。
- 返修要求：任务卡片补齐部门、岗位、培训类型、培训开始和截止时间；检查员工我的培训复用页面与主管/HR手机分支。

## 验收范围说明（续）

本轮仍继续覆盖三角色实际UI、手机卡片、八维筛选、评定流转、附件内容及权限。Office转换本机依赖准备中，不以“不支持提示”计为通过。生产账号升级与远端部署尚未执行；本地通过不代表生产已经完成。
